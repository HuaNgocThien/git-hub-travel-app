import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 12,
  },
  icon: {
    width: 40,
    height: 40,
  },
  title: {
    fontSize: 12,
    color: 'black',
    marginHorizontal: 8,
  },
});

export default styles;
