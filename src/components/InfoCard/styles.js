import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    marginTop: 12,
  },
  icon: {
    width: 32,
    height: 32,
  },
  title: {
    fontSize: 12,
    color: '#000',
    marginHorizontal: 8,
  },
});

export default styles;
