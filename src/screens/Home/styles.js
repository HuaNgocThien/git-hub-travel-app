import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    margin: 32,
  },
  subTitle: {
    fontSize: 20,
    color: 'black',
    marginTop: 40,
    marginBottom: 34,
  },
  row: {
    flexDirection: 'row',
    flexWrap: 'wrap',
    marginTop: 10,
  },
});

export default styles;
